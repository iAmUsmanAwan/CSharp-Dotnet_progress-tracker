using System.Windows;

namespace EMS_View.Views
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();    // This method connects the .xaml file to this .cs file and sets up the UI (loads controls, bindings, etc.)
        }
    }
}